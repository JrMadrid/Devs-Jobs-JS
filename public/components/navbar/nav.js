const postu = require('../../../controllers/postu/con_postulante');
// let isAdmin = false; // Prueba con true y false para ver ambos navbars
// let postu = false; // Prueba con true y false para ver ambos navbars
// let empre = false; // Prueba con true y false para ver ambos navbars
console.log(postu);

// // Esperar a que la página se cargue completamente
// document.addEventListener('DOMContentLoaded', function() { //evento que se activara cuando carge el DOm y cargara la funcion
//     // Función para cambiar la visibilidad del navbar basada en el tipo de usuario
//     function cambiarNavbar() {
//         const navbars = document.querySelectorAll('.navbari');
//         navbars.forEach(function(navbar, index) {
//             if ((isAdmin && index === 1) || (!isAdmin && index === 0)) {
//                 navbar.style.display = 'block'; // Mostrar el navbar adecuado
//             } else {
//                 navbar.style.display = 'none'; // Ocultar los otros navbars
//             }
//         });
//     }

//     // Aplicar el cambio inmediatamente
//     cambiarNavbar();
// });
